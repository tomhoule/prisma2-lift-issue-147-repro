# Migration `20190930120855-test2`

This migration has been generated by Tom Houlé at 9/30/2019, 12:08:55 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `reproambiguousselfrelations`.`A` ADD COLUMN `somethingElse` varchar(191) NOT NULL DEFAULT '' ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190930120826-test1..20190930120855-test2
--- datamodel.dml
+++ datamodel.dml
@@ -7,8 +7,9 @@
   url      = "mysql://root:prisma@127.0.0.1:3306/reproambiguousselfrelations"
 }
 model A {
-  id      String @default(cuid()) @id @unique
-  name    String
-  related A[]
+  id            String @default(cuid()) @id @unique
+  name          String
+  related       A[]
+  somethingElse String
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190930120855-test2)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190930120855-test2'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
