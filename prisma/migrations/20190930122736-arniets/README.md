# Migration `20190930122736-arniets`

This migration has been generated by Tom Houlé at 9/30/2019, 12:27:36 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `reproambiguousselfrelations`.`Member` ADD COLUMN `loginToken` varchar(191)  ,
ADD FOREIGN KEY (`loginToken`) REFERENCES `reproambiguousselfrelations`.`UserLoginToken`(`id`) ON DELETE SET NULL;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190930122704-test84892..20190930122736-arniets
--- datamodel.dml
+++ datamodel.dml
@@ -39,8 +39,9 @@
   updatedAt      DateTime       @updatedAt
   user           User
   space          Space
   role           PermissionRole
+  loginToken     UserLoginToken
   invited        Boolean?
   inviteAccepted Boolean?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190930122736-arniets)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190930122736-arniets'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
