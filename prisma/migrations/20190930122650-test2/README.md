# Migration `20190930122650-test2`

This migration has been generated by Tom Houlé at 9/30/2019, 12:26:50 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `reproambiguousselfrelations`.`Member` DROP COLUMN `role`;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190930121650-test..20190930122650-test2
--- datamodel.dml
+++ datamodel.dml
@@ -33,14 +33,13 @@
   token          String?
 }
 model Member {
-  id             String         @default(cuid()) @id @unique
-  createdAt      DateTime       @default(now())
-  updatedAt      DateTime       @updatedAt
+  id             String   @default(cuid()) @id @unique
+  createdAt      DateTime @default(now())
+  updatedAt      DateTime @updatedAt
   user           User
   space          Space
-  role           PermissionRole
   invited        Boolean?
   inviteAccepted Boolean?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190930122650-test2)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190930122650-test2'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
