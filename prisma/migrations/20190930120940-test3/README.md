# Migration `20190930120940-test3`

This migration has been generated by Tom Houlé at 9/30/2019, 12:09:40 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
DROP TABLE `reproambiguousselfrelations`.`A`;

CREATE TABLE `reproambiguousselfrelations`.`User` (
  `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `desktopIdleSeconds` varchar(191)   ,
  `displayName` varchar(191)   ,
  `doNotDisturb` boolean   ,
  `email` varchar(191) NOT NULL DEFAULT '' ,
  `id` varchar(191) NOT NULL  ,
  `name` varchar(191)   ,
  `pendingSetup` boolean   ,
  `profilePhoto` varchar(191)   ,
  `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `reproambiguousselfrelations`.`UserLoginToken` (
  `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `email` varchar(191) NOT NULL DEFAULT '' ,
  `id` varchar(191) NOT NULL  ,
  `securityPhrase` varchar(191) NOT NULL DEFAULT '' ,
  `token` varchar(191)   ,
  `verified` boolean   ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `reproambiguousselfrelations`.`Member` (
  `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `id` varchar(191) NOT NULL  ,
  `inviteAccepted` boolean   ,
  `invited` boolean   ,
  `role` varchar(191) NOT NULL DEFAULT 'Admin' ,
  `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `reproambiguousselfrelations`.`Space` (
  `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `id` varchar(191) NOT NULL  ,
  `joinSecretCode` varchar(191) NOT NULL DEFAULT '' ,
  `name` varchar(191) NOT NULL DEFAULT '' ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

ALTER TABLE `reproambiguousselfrelations`.`User` ADD COLUMN `userLoginToken` varchar(191)  ,
ADD FOREIGN KEY (`userLoginToken`) REFERENCES `reproambiguousselfrelations`.`UserLoginToken`(`id`) ON DELETE SET NULL;

ALTER TABLE `reproambiguousselfrelations`.`Member` ADD COLUMN `space` varchar(191)  ,
ADD FOREIGN KEY (`space`) REFERENCES `reproambiguousselfrelations`.`Space`(`id`) ON DELETE SET NULL,
ADD COLUMN `user` varchar(191)  ,
ADD FOREIGN KEY (`user`) REFERENCES `reproambiguousselfrelations`.`User`(`id`) ON DELETE SET NULL;

CREATE UNIQUE INDEX `User.id` ON `reproambiguousselfrelations`.`User`(`id`)

CREATE UNIQUE INDEX `User.email` ON `reproambiguousselfrelations`.`User`(`email`)

CREATE UNIQUE INDEX `UserLoginToken.id` ON `reproambiguousselfrelations`.`UserLoginToken`(`id`)

CREATE UNIQUE INDEX `Member.id` ON `reproambiguousselfrelations`.`Member`(`id`)

CREATE UNIQUE INDEX `Space.id` ON `reproambiguousselfrelations`.`Space`(`id`)
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190930120855-test2..20190930120940-test3
--- datamodel.dml
+++ datamodel.dml
@@ -6,10 +6,53 @@
   provider = "mysql"
   url      = "mysql://root:prisma@127.0.0.1:3306/reproambiguousselfrelations"
 }
-model A {
-  id            String @default(cuid()) @id @unique
-  name          String
-  related       A[]
-  somethingElse String
+model User {
+  id                 String   @default(cuid()) @id @unique
+  createdAt          DateTime @default(now())
+  updatedAt          DateTime @updatedAt
+  email              String   @unique
+  name               String?
+  displayName        String?
+  profilePhoto       String?
+  pendingSetup       Boolean?
+  doNotDisturb       Boolean?
+  desktopIdleSeconds String?
+
+  memberships Member[]
+  // invitedBy   User?
+}
+
+model UserLoginToken {
+  id             String   @default(cuid()) @id @unique
+  createdAt      DateTime @default(now())
+  email          String
+  securityPhrase String
+  user           User?
+  verified       Boolean?
+  token          String?
+}
+
+model Member {
+  id             String         @default(cuid()) @id @unique
+  createdAt      DateTime       @default(now())
+  updatedAt      DateTime       @updatedAt
+  user           User
+  space          Space
+  role           PermissionRole
+  invited        Boolean?
+  inviteAccepted Boolean?
+}
+
+enum PermissionRole {
+  Admin
+  Member
+}
+
+model Space {
+  id             String   @default(cuid()) @id @unique
+  createdAt      DateTime @default(now())
+  name           String
+  joinSecretCode String
+  members        Member[]
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190930120940-test3)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190930120940-test3'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
