# Migration `20190930120826-test1`

This migration has been generated by Tom Houlé at 9/30/2019, 12:08:26 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `reproambiguousselfrelations`.`A` (
  `id` varchar(191) NOT NULL  ,
  `name` varchar(191) NOT NULL DEFAULT '' ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

ALTER TABLE `reproambiguousselfrelations`.`A` ADD COLUMN `a` varchar(191)  ,
ADD FOREIGN KEY (`a`) REFERENCES `reproambiguousselfrelations`.`A`(`id`) ON DELETE SET NULL;

CREATE UNIQUE INDEX `A.id` ON `reproambiguousselfrelations`.`A`(`id`)
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190930120826-test1
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,14 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = "mysql"
+  url      = "mysql://root:prisma@127.0.0.1:3306/reproambiguousselfrelations"
+}
+
+model A {
+  id      String @default(cuid()) @id @unique
+  name    String
+  related A[]
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190930120826-test1)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190930120826-test1'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
