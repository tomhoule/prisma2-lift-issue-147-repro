# Migration `20190930121650-test`

This migration has been generated by Tom Houlé at 9/30/2019, 12:16:50 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `reproambiguousselfrelations`.`Member` ADD COLUMN `role` varchar(191) NOT NULL DEFAULT 'Admin' ;

ALTER TABLE `reproambiguousselfrelations`.`User` DROP COLUMN `userLoginToken`,
ADD COLUMN `userLoginToken` varchar(191)  ,
ADD FOREIGN KEY (`userLoginToken`) REFERENCES `reproambiguousselfrelations`.`UserLoginToken`(`id`) ON DELETE SET NULL;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190930121552-test5..20190930121650-test
--- datamodel.dml
+++ datamodel.dml
@@ -33,13 +33,14 @@
   token          String?
 }
 model Member {
-  id             String   @default(cuid()) @id @unique
-  createdAt      DateTime @default(now())
-  updatedAt      DateTime @updatedAt
+  id             String         @default(cuid()) @id @unique
+  createdAt      DateTime       @default(now())
+  updatedAt      DateTime       @updatedAt
   user           User
   space          Space
+  role           PermissionRole
   invited        Boolean?
   inviteAccepted Boolean?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190930121650-test)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190930121650-test'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
